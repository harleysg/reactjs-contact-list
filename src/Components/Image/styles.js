import styled from "styled-components";

export const Figure = styled.figure(() => {
  return `
    margin: 0 auto;
    overflow: hidden;
  `;
});

export const Img = styled.img`
  max-height: 100%;
  margin: auto;
`;
