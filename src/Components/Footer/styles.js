import styled from "styled-components";

export const Main = styled.footer`
  display: grid;
  min-height: calc(var(--size-min-header) * 2);
  background-color: var(--theme_bg_04);
  border-top: 1px solid var(--color_primary);
`